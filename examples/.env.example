# Uncomment and populate the values you prefer, then copy to `.env`

# GH_TOKEN=ghp_your_github_pat
# GITHUB_TOKEN=${GH_TOKEN}

# OPENAI_API_KEY=sk-your-openai-key

# GIT_AUTHOR_NAME=Your Name
# GIT_AUTHOR_EMAIL=you@example.com
# GIT_COMMITTER_NAME=${GIT_AUTHOR_NAME}
# GIT_COMMITTER_EMAIL=${GIT_AUTHOR_EMAIL}

# LOCAL_UID=1000
# LOCAL_GID=1000

# NPM cache directory for runtime-installed agents
# Point to a persistent location outside the project to preserve packages across rebuilds
# NPM_CACHE_DIR=${HOME}/.docker-npm-cache
